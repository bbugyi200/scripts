#!/bin/bash

confirm "generate_mirrors && echo"
confirm "sudo pacman -Syyu && echo"
confirm "rm -rf /tmp/builds; mkdir /tmp/builds; cd /tmp/builds && cower -vud && buildall && echo"
confirm "gem update && echo"
confirm "cd ~/.xmonad && stack install && xmonad --recompile && echo"
confirm "systemctl reboot -i"
