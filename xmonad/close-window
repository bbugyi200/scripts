#!/bin/bash
WNAME="$(xdotool getwindowfocus getwindowname)"

if wmctrl -c :ACTIVE:; then
    removeEmptyWorkspace "${WNAME}"
else
    if [[ "$(wmctrl -d | wc -l)" -le 4 ]] && [[ "$(x11screens)" -ge 3 ]]; then
        xdotool key super+bracketright
    else
        removeWorkspace
    fi
fi
