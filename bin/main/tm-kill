#!/bin/bash

socket_name="$(tm-socket)"

if [[ -n "$socket_name" ]]; then
    SESSION_NAME=$(tmux -L "$socket_name" display-message -p "#S")
    tmux -L "$socket_name" switchc -n
    tmux -L "$socket_name" kill-session -t "$SESSION_NAME"
else
    exit 1
fi
