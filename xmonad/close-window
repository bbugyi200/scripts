#!/bin/bash
WNAME=`xdotool getwindowfocus getwindowname`

if wmctrl -c :ACTIVE:; then
	changeWSifCurrEmpty $WNAME
else
    if [[ $(wmctrl -d | wc -l) -le 2 ]]; then
        xdotool key alt+s
    else
        removeWorkspace
    fi
fi
