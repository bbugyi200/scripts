#!/bin/bash

######################################
#  Update and Push Current Git Repo  #
######################################

source /home/bryan/Dropbox/scripts/modules/bash/gutils.sh

cd "$(gitdir)" || exit 1
git add .
git commit -m "Update $(grepo)"
git push
