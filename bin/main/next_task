#!/usr/bin/env python

import os
import subprocess

subprocess.call(['task', 'start.not:', 'done'])
next_task_id = os.popen("task rc.verbose=nothing rc._forcecolor=no next +READY").readline()
subprocess.call(['task', 'start', next_task_id.split()[0]])
