#!/bin/bash

############################################
#  Interface for System Maintenance Tasks  #
############################################

eval set -- "$(getopt -o "d,u,c,e" -l "debug,update,cleanup,check,ebuild-check" -- "$@")"

while [[ -n "$1" ]]; do
    case $1 in
       -d|--debug )
           PS4='$LINENO: '
           set -x;;
       -u|--update )
           update
           ;;
       -c|--cleanup )
           cleanup
           ;;
       -e )
           ebvcheck
           ;;
       --check )
           maintCheck
           ;;
       -- )
           shift
           break;;
    esac
    shift
done
