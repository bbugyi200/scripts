#!/bin/bash

PID=$$

if ping -c 1 -w 1 www.google.com &> /dev/null; then
    echo "You are already connected to the internet."
    kill $PID
fi & 

sleep 3
echo $! | xargs kill
systemctl restart netctl-auto@wlo1.service
