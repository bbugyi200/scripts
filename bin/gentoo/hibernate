#!/bin/bash

#######################
#  Wrapper for s2ram  #
#######################

# ensure running as root
if [ "$(id -u)" != "0" ]; then
  exec sudo "$0" "$@"
fi

epcsync -q
s2ram

sleep 2
rc-service net.wlo1 restart

sleep 2
rc-service dhcpcd restart
